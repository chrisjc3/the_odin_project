<h1>All Articles</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
      <%= link_to "delete", tag_path(tag), data: {confirm: "Really delete the article?"}, method: :delete %>
    </li>
  <% end %>
</ul>

